<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <title>Edit Card</title>
</head>
<body>
    <h1>Edit Card</h1>
    <form action="/admin/edit/<%= mediaItem._id %>" method="POST">
        <label for="title">Title:</label>
        <input type="text" name="title" id="title" value="<%= mediaItem.title %>" required>
        
        <label for="tags">Tags (comma separated):</label>
        <input type="text" name="tags" id="tags" value="<%= mediaItem.tags.join(',') %>" required>
        
        <label for="group">Group:</label>
        <input type="text" name="group" id="group" value="<%= mediaItem.group %>">
        
        <label for="type">Type:</label>
        <select name="type" id="type" required>
            <option value="video" <%= mediaItem.type === 'video' ? 'selected' : '' %>>Video</option>
            <option value="image" <%= mediaItem.type === 'image' ? 'selected' : '' %>>Image</option>
        </select>
        
        <label for="link">Link:</label>
        <input type="text" name="link" id="link" value="<%= mediaItem.link %>" required>
        
        <button type="submit">Edit Card</button>
    </form>
</body>
</html>
